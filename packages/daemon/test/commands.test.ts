import assert from"node:assert/strict";import{test}from"node:test";import{CommandRouter}from"../src/command-router.js";
test("commands route only to an authorized live socket and duplicate identity conflicts",()=>{const sent:any[]=[];const r=new CommandRouter();r.bind("p","i",x=>sent.push(x),()=>true);const c={commandId:"c",processId:"p",processInstanceId:"i",payload:{type:"abort"}};assert.equal(r.accept("principal",c).duplicate,false);assert.equal(r.accept("principal",c).duplicate,true);assert.throws(()=>r.accept("other",c),/command_conflict/);assert.equal(sent.length,1);r.disconnect("p","i");assert.equal(r.state("c"),"failed:bridge_disconnected");});
